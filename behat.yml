default:
    autoload:
      - %paths.base%/tests/features/bootstrap

    suites:
        cli:
            paths:
              - %paths.base%/tests/features
            contexts:
              - CommandContext
              - FilesystemContext
              - PackageContext
              - RepositoryContext
