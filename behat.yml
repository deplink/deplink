default:
    autoload:
      - %paths.base%/tests

    suites:
        console:
            paths:
              - %paths.base%/tests/Console
            contexts:
              - Deplink\Tests\Console\CommandContext
              - Deplink\Tests\Environment\FilesystemContext
