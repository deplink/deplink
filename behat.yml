default:
    autoload:
      - %paths.base%/tests/features/bootstrap

    suites:
        cli:
            paths:
              - %paths.base%/tests/features
            filters:
              tags: ~@unimplemented
            contexts:
              - CommandContext
              - FilesystemContext
              - PackageContext
              - RepositoryContext
